const uiConfig = {
  useUnifiedProgressBar: true,
  // true  = use global ProgressBar for all progress screens
  // false = use TravelProgressView's original local progress bar
};

export default uiConfig;